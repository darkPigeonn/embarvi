import 'package:embarvi/utils/colorLib.dart';
import 'package:flutter/material.dart';

const title = TextStyle(fontFamily: 'Arturo', fontSize: 30, color: textPrimary);
const titleApp =
    TextStyle(fontFamily: 'Intro', fontSize: 40, color: textPrimary);
const subTitle =
    TextStyle(fontFamily: 'Arturo', fontSize: 20, color: textPrimary);
